{
  "name": "obsidian-task-master",
  "version": "0.0.1",
  "description": "A single full-page view over every task in the vault.",
  "main": "main.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run check && node esbuild.config.mjs production",
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.tests.json && svelte-check --tsconfig ./tsconfig.json --threshold error",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "Jon Yardley",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^26.1.2",
    "builtin-modules": "^5.3.0",
    "esbuild": "^0.28.1",
    "esbuild-svelte": "^0.9.5",
    "obsidian": "^1.13.1",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.4",
    "svelte-preprocess": "^6.0.5",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@atlaskit/pragmatic-drag-and-drop": "^2.0.1"
  }
}
